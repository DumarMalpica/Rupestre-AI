# ── API ──────────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ── Core ─────────────────────────────────────────────────────────────────────
pydantic-settings>=2.4.0
langgraph>=0.2.0
python-dotenv>=1.0.0

# ── Observabilidad ────────────────────────────────────────────────────────────
langfuse>=4.0.0
langsmith>=0.1.0            # trazas del grafo LangGraph (auto-instrumentado)

# ── Procesamiento de imagen ───────────────────────────────────────────────────
# Headless: sin dependencias de GUI (apto para servidor)
opencv-python-headless>=4.9.0
numpy>=1.26.0
Pillow>=10.0.0

# ── Generación de PDF ─────────────────────────────────────────────────────────
reportlab>=4.0.0
jinja2>=3.1.0

# ── Vector store y corpus ─────────────────────────────────────────────────────
pymongo>=4.6.0             # MongoDB (Railway) — almacena vectores del corpus
sentence-transformers>=3.0.0  # embeddings multilingües (MiniLM, 384 dim)
PyMuPDF>=1.24.0            # fitz — carga PDFs del corpus

# ── LLM providers ─────────────────────────────────────────────────────────────
requests>=2.31.0            # ollama provider
langchain-openai>=0.1.0     # openai provider
anthropic>=0.69.0           # anthropic provider — Claude visión + interpretación

# ── HTTP client ───────────────────────────────────────────────────────────────
httpx>=0.27.0               # descarga de imágenes WhatsApp

# ── WhatsApp / Twilio ─────────────────────────────────────────────────────────
twilio>=8.0.0

# ── Modelos AI / YOLO ─────────────────────────────────────────────────────────
ultralytics>=8.3.0

# ── Inpainting / LaMa ─────────────────────────────────────────────────────────
# LaMa inpainting via lama-cleaner (instalar con --no-deps):
#   pip install lama-cleaner --no-deps
#   pip install loguru yacs omegaconf einops timm scikit-image
# Las dependencias conflictivas (flask, diffusers, transformers, etc.)
# no son necesarias — solo se usa el modelo LaMa en CPU
loguru
yacs
omegaconf
einops
timm
scikit-image
