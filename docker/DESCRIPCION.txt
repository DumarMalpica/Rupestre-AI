CARPETA: docker/
═══════════════════════════════════════════════════════════════

RESPONSABLE: Compartido

PROPÓSITO
─────────
Dockerfiles separados por servicio para construcción de imágenes.
Referenciados desde docker-compose.yml en la raíz.

ARCHIVOS QUE DEBE CONTENER
───────────────────────────

api.Dockerfile
  IMAGEN BASE : python:3.12-slim (liviana, sin GPU)
  CONTIENE    : FastAPI + Uvicorn + dependencias de la API
  NO CONTIENE : PyTorch, OpenCV (eso va en agents.Dockerfile)
  PUERTO      : 8000
  TAMAÑO      : ~500MB (liviana intencionalmente)

agents.Dockerfile
  IMAGEN BASE : python:3.12 (completa, con compiladores)
  CONTIENE    : PyTorch, OpenCV, Ultralytics, LangChain,
                CrewAI, LangFuse y todas las dependencias
                de los 6 agentes
  DEPENDENCIAS DE SISTEMA:
    libgl1-mesa-glx  → requerida por OpenCV en Linux
    libglib2.0-0     → requerida por OpenCV en Linux
  TAMAÑO      : ~5-8GB (incluye PyTorch con CUDA)

models.Dockerfile  ← PENDIENTE CREAR (opcional)
  IMAGEN BASE : python:3.12 con CUDA
  PROPÓSITO   : Servir inferencia del GAN en GPU dedicada
                separada del resto de los servicios
